#include <stdio.h>
#include <conio.h>

int  main(){
    int a;
    printf("enter the num");
    scanf("%d",&a);
    for (int i = 0; i < a; i++)
    {
        printf("number is %d \n",i);
    }
    return 0;
}
